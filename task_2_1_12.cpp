#include <bits/stdc++.h>
using namespace std;
int main(){
    int a, b, c, d, e, f, g;
    cin >> a >> b >> c >> d >> e >> f >> g;
    cout << 4*(a+b)*(a+b) - (c-d)*(c-d) + (f+2*g-e)*(f+2*g-e)*(f+2*g-e);
    return 0;
}
